extends ../layout


block prepend title
  | #{ page.title + " - "}

block header
  h1 #{page.title} (archives)
  
block content
  article.article
    - var content = page.metadata['content:encoded']
    section.content!= content
